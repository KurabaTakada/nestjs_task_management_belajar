export enum ErrorMessages {
    PASSWORD_PATTERN_NOT_MATCH = 'Password must contain one upper letter and one number',
    USER_ALREADY_EXISTS = 'User already exists',
}

export enum AUTH_VAR {
    SECRET = 'kicoetcoet123',
}
